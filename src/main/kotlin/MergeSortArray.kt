class MergeSortArray {
    fun merge(nums1: IntArray, m: Int, nums2: IntArray, n: Int): Unit {
        var num1List = nums1.asList().toMutableList()
        var num2List = nums2.asList().toMutableList()
    }
}